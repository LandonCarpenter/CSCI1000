#include <iostream>
#include <iomanip>
#include <cmath> // sqrt, pow

using namespace std;

// takes something and turns it into to something else, input output

// 2 types of functions
// built-in function -> written by someone else and we can access that through
// external libraries that we add using the include directive
int main() // main function, () usually means function

{
    // setpricision()

    //double number = 0;

    //cout << "Enter a number:" << endl;
   //  cin >> number;
    // 2 ^ 3 = 8
    // pow(base, exponent);
    double answer = pow(7,-2); // function call
    // functionName(function arguments/actual parameters/function inputs)
    // input and output (most cases)

    cout << answer << endl;


    return 0;
}